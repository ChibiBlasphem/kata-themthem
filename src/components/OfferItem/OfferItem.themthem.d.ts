interface ComponentDesignTokenBox {
  OfferItem: {
    colors: {
      $values: ['title', 'text'];
      background: {
        $modifiers: ['hover'];
      };
      border: {
        $modifiers: ['hover'];
      };
      chip: {
        $values: ['background', 'text'];
      };
    };
  };
}
